(function(){
'use strict';
if(!window.H2FlowGuard)return;const G=H2FlowGuard;
function storageMap(){try{const el=document.getElementById('pm');if(el)return JSON.parse(el.textContent).storage||{}}catch(e){}return {1:'history2-day1-original-mastery-v2',2:'history2-day2-attention-mastery-v2',3:'history2-day3-terms-mastery-v1',4:'history2-day4-terms-mastery-v1',5:'history2-day5-terms-mastery-v1',6:'history2-day6-unit-review-v1'}}
function add(){if(document.getElementById('flowGuardParent'))return;const content=document.getElementById('content');if(!content)return;const rep=G.report(storageMap()),events=rep.recentEvents||[],recoveries=events.filter(x=>/auto-recovery|state-sanitized|dead-end|error/.test(x.type));const last=recoveries[recoveries.length-1];const html=`<section class="section" id="flowGuardParent"><h2>흐름 안정성</h2><div class="desc">학생 화면의 빈 화면·진행 불가 상태를 자동 감지하고 마지막 정상 단계로 복구합니다.</div><div class="schedule"><div class="bucket"><span class="lab">최근 진단 이벤트</span><b>${events.length}</b><span>최대 120건 보관</span></div><div class="bucket"><span class="lab">자동 복구·수리</span><b>${recoveries.length}</b><span>${last?new Date(last.at).toLocaleString():'기록 없음'}</span></div><div class="bucket"><span class="lab">Guard 버전</span><b style="font-size:18px">${G.VERSION}</b><span>원문 DB와 분리 저장</span></div></div><div class="foot"><button class="btn" id="exportFlowDiagnostic">진단 보고서 내보내기</button></div></section>`;content.insertAdjacentHTML('beforeend',html);document.getElementById('exportFlowDiagnostic')?.addEventListener('click',()=>G.downloadReport(storageMap(),'history2-flow-diagnostic-'+new Date().toISOString().slice(0,10)+'.json'))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(add,40));else setTimeout(add,40);new MutationObserver(()=>add()).observe(document.documentElement,{childList:true,subtree:true});
})();
