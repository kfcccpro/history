# HISTORY2 Focus UX V6.2 검증 결과

작업일: 2026-08-15

## 자동/정적 검증

- JavaScript 구문 검사: 프로젝트 내 `.js` 51개 `node --check` 통과.
- CSS 구문 검사: `.css` 2개 PostCSS parse 통과.
- Day 1~6 `courseData` JSON 파싱 통과.
- 원문 문제 수: 140문항 유지.
- 개념 수: 56개. Day 6 p15 분리를 위해 V6의 55개에서 1개 증가.
- 원문 이미지 수: 150개 유지.
- 중복 concept ID: 없음.
- 문제 → concept 미존재 참조: 없음.
- memoryCheck → 문제 concept 불일치: 없음.
- U+FFFD(깨진 문자) 검출: 없음.
- Day 6 p15의 예전 `direct-action` 매핑 잔존: 없음.
- 정확 문자열 `conceptTitle: 의열 투쟁` / `민족 유일당과 학생 운동` 잔존: 없음.

## V6.2 UX 코드 검증

- 4열 합계 100% + gap에 의한 우측 패널 overflow 위험 제거.
- 첫 핵심어 공개 행동 1개만 활성화되는 상태 로직 확인.
- 이후 핵심어는 설명 카드에서 한 단계씩 공개되는 상태 로직 확인.
- 회상 입력은 공개된 개념 핵심어 목록에 대한 정규화 정확 일치로 확인.
- 선택 가지/현재 키워드 내부 스크롤 보정 로직 확인.
- SVG guide 자체 mutation을 observer가 다시 처리하지 않도록 필터 확인.

## 남은 실기 확인

이 실행 환경에서는 Chromium 기반 실제 화면 렌더 자동화가 정상 실행되지 않아 브라우저 시각 회귀 검사는 제외했다.
GitHub Pages 반영 후 PC 또는 태블릿 가로 화면에서 아래 3가지만 최종 확인하면 된다.

1. 4열의 `설명 → 직접 입력`이 오른쪽에서 잘리지 않는지.
2. 첫 핵심어 → 다음 핵심어 → 직접 입력의 시선이 왼쪽에서 오른쪽으로 이어지는지.
3. Day 6 p15를 틀렸을 때 `1920년대 무장 독립 전쟁 / 시대·순서 연결`로 진단되는지.
