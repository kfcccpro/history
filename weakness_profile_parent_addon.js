(function(){
'use strict';
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function rows(items,kind){return (items||[]).filter(x=>x.score>0).map(x=>`<div class="day"><div class="n">${esc(kind)}</div><b>${esc(x.name)}<small style="display:block;color:var(--muted);margin-top:5px;font-size:13px">${x.count}회 · ${x.dayCount}개 Day${x.examples&&x.examples.length?' · 예: '+esc(x.examples[0]):''}</small></b><span class="pill hot">재확인</span></div>`).join('')}
function render(){if(!window.H2WeaknessProfile)return;const content=document.getElementById('content');if(!content||document.getElementById('parentWeaknessMap'))return;const p=H2WeaknessProfile.get();if(!(p.topDepth||[]).some(x=>x.score>0)&&!(p.topConcept||[]).some(x=>x.score>0))return;const sec=document.createElement('section');sec.id='parentWeaknessMap';sec.className='section';sec.innerHTML=`<h2>Day 1~18 통합 취약점 지도</h2><div class="desc">정답률보다 반복 오답의 위치를 봅니다. 단원·개념·Depth를 같은 기준으로 누적합니다.</div><h3 style="margin:18px 0 9px;font-size:18px">Depth 분포</h3><div class="days">${rows(p.topDepth,'Depth')||'<div class="sub">진단 기록이 없습니다.</div>'}</div><h3 style="margin:22px 0 9px;font-size:18px">반복된 개념</h3><div class="days">${rows((p.topConcept||[]).slice(0,6),'개념')||'<div class="sub">반복 오답이 없습니다.</div>'}</div><h3 style="margin:22px 0 9px;font-size:18px">단원별 집중 구간</h3><div class="days">${rows(p.topUnit,'단원')||'<div class="sub">누적 데이터가 없습니다.</div>'}</div>`;content.appendChild(sec)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(render,30),{once:true});else setTimeout(render,30);window.addEventListener('history2:cloud-reconciled',()=>setTimeout(()=>{const old=document.getElementById('parentWeaknessMap');if(old)old.remove();render()},80));
})();