# 새 채팅 시작점 — 한국사Ⅱ 학습 웹앱

새 프로젝트 채팅에서 사용자가 **`다음 작업 진행`**, **`진행`**이라고 입력하면 질문하지 말고 아래 순서로 바로 진행한다.

## 1. 반드시 먼저 읽을 파일

GitHub 저장소 `kfcccpro/history`의 `main`에서 다음 두 파일을 먼저 읽는다.

1. `NEXT_CHAT_START_HERE.md` — 현재 문서
2. `CURRENT_RELEASE_STATE.json` — 기계 판독용 최신 상태

그 다음 필요한 경우 아래 핵심 파일만 확인한다.

- `fast_day.html`
- `fast_day_engine.js`
- `sync_local_hook.js`
- `depth_choice_mode_addon.js`
- `concept_depth_day_addon.js`
- `flow_guard_day_addon.js`

## 2. 프로젝트 핵심 목적

고1 ADHD적 특성을 고려한 **문제 중심 한국사Ⅱ 학습 웹앱**이다.

핵심 원칙:

- 맞고 틀림만 보여주지 않는다.
- 오답이면 학생이 **어디서부터 모르는지** 확인하게 한다.
- 필요한 개념 깊이만 복구한다.
- 복구 후 같은 원문제를 다시 푼다.
- 학생 UI는 한 화면 한 행동, 큰 글씨, 낮은 시각 분산을 우선한다.

## 3. 현재 배포/접속

- GitHub repo: `kfcccpro/history`
- branch: `main`
- 기본 사이트: `https://kfcccpro.github.io/history/`
- 학생 시작 페이지: `https://kfcccpro.github.io/history/fast_index.html`
- 배포: `main` → GitHub Pages
- Firebase Hosting / Cloud Shell은 사용하지 않는다.
- Firebase는 진도·오답·학습 이력의 기기 간 동기화만 담당한다.
- 앱 모드: `release`
- 학생 PIN: `8081`
- 관리자 PIN: `2007`

## 4. 직전 완료된 핵심 작업

### Day 1~6

- D2/D3/D4 진단을 실제 문제 요구와 대조해 주요 오진 수정.
- 140문항 개념 뎁스 경로를 공통 진단 기준과 정합화.
- 기존 중간 checkpoint를 제거하고 **Depth 복구 완료 → 같은 원문 문제 즉시 재도전**으로 수정.
- 최종 핵심어 단계에서 긴 주관식 입력을 최대한 줄이라는 사용자 요청을 반영해, `depth_choice_mode_addon.js`로 **4지선다 선택 방식**을 추가함.
- `sync_local_hook.js`는 원래 저장/Firebase 동기화 로직을 그대로 보존하고 객관식 애드온 로더만 추가한 상태.
- 객관식 애드온 최신 커밋: `2838e04f7d2ca3f75167eb232e6fde84120eb675`
- 해당 커밋 GitHub Pages 빌드 성공 확인.

### 전체 흐름

- Day 1~6: 문제 → 오답 진단 → 적응형 Depth 복구 → 마지막 선택 확인 → 원문제 재도전
- Day 7~18: 빠른 경량 흐름 유지
- release 진도 저장/Firebase 동기화 유지

## 5. 새 채팅에서 가장 먼저 해야 할 작업 — 이것이 현재 1순위

사용자의 가장 최근 요청:

> **`시인성 지금보다 2배더 키워줘.`**

대상은 사용자가 마지막으로 첨부한 **Day 7~18 빠른 학습 시작 화면**이다.

화면 특징:

- 제목: `큰 흐름만 보고 바로 문제로 들어갑니다.`
- 5개 큰 흐름 카드
- `틀린 문제만 쉽게 복구하고 다시 풉니다.` 안내
- `문제 시작` 버튼
- `전체 Day 목록`

### 매우 중요

**이 시인성 2배 확대 요청은 아직 실제 웹앱 코드에 적용되지 않았다.**

직전 채팅에서 확대 예시 이미지는 생성했지만, 그것은 디자인 참고 이미지일 뿐이다. 실제 `fast_day.html` CSS는 아직 수정하지 않았다.

### 새 채팅의 첫 실제 코드 작업

`fast_day.html`을 기준으로 다음을 적용한다.

1. 데스크톱/태블릿에서 시작 카드가 화면을 훨씬 넓고 크게 사용하게 한다.
2. 제목, 5개 branch 카드의 번호/텍스트, 안내 문장, `문제 시작`, `전체 Day 목록`의 **체감 시인성을 현재의 약 2배 수준으로 올린다.**
3. 단순히 CSS `transform:scale(2)`를 쓰지 말고 폰트·간격·카드 높이·최대 폭을 실제 레이아웃 값으로 확대한다.
4. 5개 카드는 데스크톱에서 한 줄을 유지하되 글자가 충분히 크고 카드 높이가 확보되게 한다.
5. 모바일에서는 1~2열로 자연스럽게 재배치하고 가로 스크롤이 생기지 않게 한다.
6. `문제 시작` 버튼은 훨씬 크고 명확한 1차 행동으로 보이게 한다.
7. 다른 학습 흐름, 문제 데이터, Firebase, 진도 저장은 건드리지 않는다.

수정 후에는 해당 파일 중심으로 최소 회귀검증하고 GitHub Pages 배포 성공을 확인한다.

## 6. 종료/작업 범위 원칙

광범위한 재감사나 새로운 기능 추가를 다시 시작하지 않는다.

현재는 완성본에서 **사용자가 명시적으로 요청한 UX 수정만** 처리하는 유지보수 단계다.

따라서 새 채팅에서:

- 140문항을 다시 전수감사하지 않는다.
- D값을 다시 처음부터 검토하지 않는다.
- Firebase 구조를 다시 설계하지 않는다.
- 관리자 기능을 새로 만들지 않는다.
- 사용자가 요청하지 않은 기능을 추가하지 않는다.

## 7. GitHub 작업 방식

가능하면 사용자가 ZIP을 내려받아 덮어쓰게 하지 않는다.

- GitHub `main`의 최신 파일을 먼저 읽는다.
- 필요한 파일만 최소 수정한다.
- GitHub `main`에 직접 반영한다.
- GitHub Pages 성공 여부를 확인한다.
- 사용자가 GitHub Desktop에서 Commit/Push할 필요가 없도록 한다.

## 8. 백업 규칙

이번 새 채팅 인계를 위해 별도 전체 ZIP은 만들지 않는다.

GitHub `main`이 최신 소스의 기준점이다.

전체 ZIP은 사용자가 명시적으로 요청할 때만 만든다.

## 새 채팅에서 사용자에게 필요한 입력

사용자는 새 채팅에서 아래 한 문장만 입력하면 된다.

> **`다음 작업 진행`**

그러면 이 문서와 `CURRENT_RELEASE_STATE.json`을 읽고, 즉시 **Day 7~18 시작 화면 시인성 약 2배 확대 작업**부터 진행한다.
