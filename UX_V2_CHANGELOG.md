# History2 UX v2 변경 사항

## 화면/기기

- PC·태블릿: 가로 화면 우선 2열 문제 배치
- 모바일·태블릿 터치 기기: 세로 화면에서는 가로 회전 안내 화면으로 학습 UI를 보호
- PWA manifest orientation을 `landscape`로 설정
- 문제 원문 이미지는 좌측, 번호/입력/정답 확인은 우측에 배치
- 작은 가로 화면에서도 현재 위치 breadcrumb를 짧게 유지
- 문제 원문 이미지를 클릭/탭하면 확대 보기

## 초반 마인드맵

- 단순 미리보기 대신 중심 개념에서 가지가 뻗는 radial mind-map 형태
- 가지를 클릭하면 하단에 그 가지의 개념 카드 표시
- 각 가지별 `이 가지 확인 완료`를 직접 눌러야 완료 처리
- 모든 필수 가지 확인 전에는 `문제 시작` 버튼 비활성화
- 확인 상태는 기존 localStorage/Firebase 동기화 계층에 포함됨

## 오답 복구 키워드

- 인과/연표 키워드를 처음부터 전부 표시하지 않음
- 첫 연결만 보여준 뒤 `다음 연결 나타내기`를 눌러 하나씩 공개
- 새로 나타나는 핵심어에 짧은 1회 애니메이션 적용
- `prefers-reduced-motion`에서는 애니메이션 제거

## 학습 데이터 보호

- Day 1~6 courseData 문제/정답/피드백 원문은 변경하지 않음
- 기존 오답 → 메타인지 → 복구 → 원문 재도전 흐름을 유지
- Firebase 설정 및 Firestore 규칙 유지
