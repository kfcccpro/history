# HISTORY2 Focus UX V6.2 변경 사항

기준: `HISTORY2_MASTER_BACKUP_2026-08-10_2051_V6.zip`
작업일: 2026-08-15

## 1. Focus UX

- PC/태블릿 가로 화면에서 4열 지도가 실제 가용 폭을 더 넓게 사용하도록 조정.
- 기존 `%` 열 너비 합계 100% + gap 구조를 `fr/minmax()` 구조로 변경하여 우측 `설명 → 직접 입력` 영역 잘림 가능성 제거.
- 큰 가지, 핵심어, 설명, 회상 입력의 글자 크기와 클릭 영역 확대.
- 첫 행동을 `3. 핵심어` 열의 `첫 핵심어 나타내기` 하나로 제한.
- 이후 핵심어는 우측 설명 카드의 `다음 핵심어 나타내기 →` 한 행동으로 순차 공개.
- 선택한 큰 가지/현재 핵심어가 내부 스크롤에서 벗어나면 자동으로 가운데 쪽에 오도록 보정.
- 회상 입력 단계가 열리면 입력창에 자동 포커스.
- 연결선은 얇게 하여 글자보다 시각적 우선순위가 낮도록 조정.
- SVG 연결선 갱신이 MutationObserver를 다시 자극하는 반복 렌더 가능성 차단.

## 2. 능동 회상 판정

- 보이는 핵심어 중 하나를 실제로 회상해 입력해야 통과하도록 판정 강화.
- 부분 문자열만 맞는 느슨한 통과를 제거하고, 공백/문장부호를 제외한 정확 일치 방식으로 변경.

## 3. Day 6 개념 정밀도 수정

- `독립 의군부·의열단·한인 애국단`을 다루는 범주를 `비밀 결사와 의열 투쟁`으로 명확화.
- `물산 장려 운동·신간회·광주 학생 항일 운동`을 한 범주에서 다룰 때 제목을 `실력 양성·민족 협동·학생 운동`으로 정밀화.
- Day 6 p15 `독립군 전투의 순서`가 `의열 투쟁`으로 진단되던 잘못된 연결을 분리.
- 새 개념 `1920년대 무장 독립 전쟁`을 추가하고 p15를 해당 개념으로 연결.
- p15 오답 진단을 `D3 시대·순서 연결`로 조정하여 `봉오동 → 청산리 → 자유시 → 독립군 재편`의 시간 흐름을 복구하도록 변경.
- 관련 개념 깊이 DB, 학습 패턴 DB, 학생/보호자 화면의 파생 매핑을 함께 동기화.
- p11 오답 설명에서 인명 `조명하`가 동사처럼 읽힐 수 있는 문장을 `인물 ‘조명하’`로 명확화.

## 4. 유지한 원칙

- Day 1~6 원문 문제 140문항 유지.
- 원문 문제 이미지 유지.
- 오답 → 뎁스 진단 → 핵심 복구 → 직접 회상 → 원문 재도전 흐름 유지.
- 개발 중 Firebase Hosting/Cloud Shell 배포 절차를 추가하지 않음.
- 기존 GitHub Pages 배포 구조 유지.
