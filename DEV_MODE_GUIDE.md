# History2 개발 모드 v5

현재 `app_mode_config.js`의 `buildMode`는 `development`입니다.

## 개발 중
- 관리자 PIN `2007` -> `dev.html` 개발 테스트 콘솔
- 원하는 Day / 문항 직접 이동
- 문제 / 오답 / 복구 단계 / 정답 화면 즉시 확인
- 개발 콘솔에서 열린 학습은 진도 및 학습이력 저장 안 함
- Firebase 동기화 안 함

학생 PIN `8081`은 학생 시작 화면 확인용입니다.

## 최종 학생 배포 전
`app_mode_config.js`의 `buildMode`를 `release`로 바꾼 뒤 아래 체크리스트를 검증합니다.

- 학생 PIN 8081
- 관리자 PIN 2007
- 학생 마지막 진도 저장 및 재접속 복원
- 문제별 정답/오답 시도 이력
- 오답 진단 Depth
- 키워드 직접 입력 기록
- 복구 학습 완료 기록
- 원문 문제 재도전 결과
- Firestore 저장 및 다른 기기에서 복원
- 관리자 화면에서 학생 이력 확인
