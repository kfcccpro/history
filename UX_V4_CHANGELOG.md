# History2 Focus UX v4

- 문제 화면에서 좌측 세로 경로 레일을 숨겨 원문 문제에 화면 폭을 우선 배정했습니다.
- 문제 원문 이미지를 고정된 뷰포트 안에서 `object-fit: contain`으로 강제해, 원문 전체가 잘리지 않고 보이게 했습니다.
- 단일 원문 이미지는 intrinsic size가 grid를 밀어내지 못하도록 absolute-fit 방식으로 처리했습니다.
- 정답 입력 영역을 오른쪽 answer dock으로 묶어 큰 빈 공간을 줄였습니다.
- 현재 문제의 `문제편 페이지/문항 번호 + 제목 + 오늘 순서`를 답안 영역에 항상 표시합니다.
- 저장된 진도 때문에 중간 문항에서 재개된 경우, 왜 해당 번호부터 시작했는지 화면에서 직접 설명합니다.
- 필요하면 `1번부터 다시 풀기`로 위치만 1번으로 돌릴 수 있으며 기존 오답/학습 이력은 지우지 않습니다.
- Day 1~6 기존 courseData와 140문항 원문/정답/피드백 데이터는 변경하지 않았습니다.
