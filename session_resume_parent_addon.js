(function(){
'use strict';if(!window.H2SessionResume)return;
const s=H2SessionResume.parentSnapshot(),a=s.aggregate;
function pct(v){return v==null?'-':Math.round(Number(v)*100)+'%'}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function dt(v){return v?new Date(v).toLocaleString('ko-KR',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'}):'-'}
const css=`.resume-parent{margin:22px 0;padding:20px;border:1px solid #dbe4ee;border-radius:18px;background:#fff}.resume-parent h2{margin:0 0 8px;font-size:22px}.rp-kpis{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px;margin:14px 0}.rp-kpi{padding:13px;border-radius:14px;background:#f5f8fb}.rp-kpi span{display:block;font-size:11px;font-weight:850;color:#6b788a}.rp-kpi b{display:block;font-size:22px;margin-top:4px}.rp-table{width:100%;border-collapse:collapse;font-size:13px}.rp-table th,.rp-table td{padding:9px;border-bottom:1px solid #e7edf3;text-align:left;vertical-align:top}.rp-table small{display:block;color:#7a8797;margin-top:3px}@media(max-width:760px){.rp-kpis{grid-template-columns:repeat(2,1fr)}}`;const st=document.createElement('style');st.textContent=css;document.head.appendChild(st);
const rows=s.rows.slice(0,12).map(x=>`<tr><td>${esc(dt(x.endedEarlyAt||x.createdAt))}<small>Day ${x.day} · ${esc(x.fromProblemId)} → ${esc(x.nextProblemId)}</small></td><td>${x.completedProblems}/${x.plannedProblems}<small>${pct(Number(x.completedProblems||0)/Math.max(1,Number(x.plannedProblems||1)))}</small></td><td>${esc((x.reasons||[]).join(' · ')||'-')}</td><td>${x.resumeStartedAt?'시작':'대기'}<small>${x.anchorCompletedAt?'핵심 연결 완료':'-'}</small></td><td>${x.targetCompletedAt?'완료':'-'}<small>${x.targetFirstAttemptAt?(x.targetFirstAttemptCorrect?'첫 시도 정답':'첫 시도 오답'):'-'}</small></td></tr>`).join('');
const html=`<section class="resume-parent"><h2>세션 종료 · 재개</h2><div class="rp-kpis"><div class="rp-kpi"><span>자동 종료</span><b>${a.earlyEnds}</b></div><div class="rp-kpi"><span>재개 시작</span><b>${a.resumeStarted}</b></div><div class="rp-kpi"><span>재개 완료율</span><b>${pct(a.resumeCompletionRate)}</b></div><div class="rp-kpi"><span>재개 첫 정답률</span><b>${pct(a.firstAttemptAccuracy)}</b></div><div class="rp-kpi"><span>계획 대비 종료 시점</span><b>${pct(a.avgPlanRatio)}</b></div></div><div style="overflow:auto"><table class="rp-table"><thead><tr><th>종료 시점</th><th>실제/계획</th><th>종료 근거</th><th>재개</th><th>지정 문제</th></tr></thead><tbody>${rows||'<tr><td colspan="5">아직 자동 종료 기록이 없습니다.</td></tr>'}</tbody></table></div><p style="font-size:12px;color:#758295;line-height:1.55">최소 4개 서로 다른 원문 문제를 완료한 뒤에만 종료 후보를 평가하고, 24시간 최대 2회·4시간 쿨다운을 적용합니다. 오답 복구 도중에는 종료하지 않습니다.</p></section>`;
const foot=document.querySelector('.foot,.footer');if(foot)foot.insertAdjacentHTML('beforebegin',html);else(document.getElementById('content')||document.querySelector('.wrap')||document.body).insertAdjacentHTML('beforeend',html);
})();
