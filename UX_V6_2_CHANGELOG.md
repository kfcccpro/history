# History2 Focus UX v6.2

Date: 2026-08-10

## Purpose

V6.1의 실제 1600×900 및 태블릿 가로 렌더를 점검한 뒤, 화면 높이 계산·오답 Depth 시인성·Day 1 학습 지도 분류·학생형 런타임 결함을 함께 보정했다.

## Applied changes

### 1. 실제 가로 뷰포트 안에 핵심 화면 고정

- 마인드맵, 문제 패널, 오답 피드백 화면의 높이를 실제 콘텐츠 시작점에 맞춰 다시 계산했다.
- 1600×900, 1180×720, 1024×768 가로 화면에서 body overflow가 발생하지 않도록 보정했다.
- 문제 원문 이미지와 답안 선택 영역이 한 화면 안에서 최대한 함께 보이도록 유지했다.

### 2. 오답 Depth 화면 시인성 강화

- Depth 제목, 단계 경로, 선택 버튼, 설명, 직접 입력의 글자 크기와 터치 높이를 상향했다.
- 사용자에게 어색한 `소개념` 표현을 `세부 개념` 또는 자연어 질문으로 변경했다.
- 내부 Depth 메타데이터의 단계 이름도 `세부 개념`으로 통일했다.

### 3. Day 1 마인드맵 분류 보정

- 첫 큰 가지 이름을 `국제 정세`에서 `국제 배경·해외 동포`로 변경했다.
- 힌트를 `제1차 세계 대전 · 러시아 혁명 · 하와이 이주`로 변경했다.
- 목적: 같은 가지 아래 배치된 `하와이 이주 동포`가 단순 국제 정세로 오해되는 의미 불일치 해소.
- 문제, 정답, 핵심어, 개념 본문은 변경하지 않았다.

### 4. 학생용 Depth 완료 런타임 결함 수정

- 공통 Depth 모듈이 학생형에는 없는 `metacogFor()`를 완료 단계에서 직접 호출하던 의존성을 제거했다.
- 학생형에서도 마지막 핵심어 직접 입력 후 Depth 로그가 정상 기록된다.

### 5. Day 6 학생형 문제 전환 함수 복구

- 누락되어 있던 `resetRuntimeForQuestion`, `nextProblem`, `retryCurrent`를 복구했다.
- Day 6에서 다음 문제 이동·현재 문제 재시도·Depth 완료가 실제 실행되도록 수정했다.

## Validation

- Day 1~6 학생 문제: 140문항
- 개념: 55개
- 구조 오류: 0
- 경고: 0
- 독립 JS 51개 `node --check`: 오류 0
- V6.1 대비 문제·정답·핵심어·개념 본문 변경 없음
- Day 1: 1600×900 / 1180×720 / 1024×768 body overflow 0
- Day 1 Depth 마지막 직접 입력 완료 → 로그 기록 정상
- Day 6 실제 렌더 및 Depth 완료 → 애플리케이션 런타임 예외 0, 로그 기록 정상
- 닫힌 용어 카드는 의도된 off-canvas drawer이며 body 스크롤을 만들지 않는다.

세부 결과는 `V6_2_VALIDATION_REPORT.md`와 `handoff_evidence/v6_2_qa/`를 참고한다.
